// stb_ds's implementation block is not covered by the header's include guard,
// so it has to be expanded in exactly one TU that includes nothing else.
#define STB_DS_IMPLEMENTATION
#include "system/stb_ds.h"
